global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "nomad"
    metrics_path: "/v1/metrics"
    params:
      format:
        - "prometheus"
    static_configs:
      - targets:
          - "127.0.0.1:4646"
